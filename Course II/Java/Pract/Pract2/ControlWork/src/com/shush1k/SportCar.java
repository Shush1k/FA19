package com.shush1k;

public class SportCar extends Car{

    SportCar(String color, int maxSpeed, String type, int currSpeed, int price, String brand) {
        super(color, maxSpeed, type, currSpeed, price, brand);
    }

}